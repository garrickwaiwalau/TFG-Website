<div><h1>Articles</h1></div>

Our blog has <%= Index.public_count %> articles and counting!

<div>
      <ul>
        <% @articles.each do |article| %>
          <% unless article.archived? %>
            <li>
              <%= link_to article.title, article %>
            </li>
          <% end %>
        <% end %>
      </ul>
</div>

<%= link_to "New Index", new_article_path %>
